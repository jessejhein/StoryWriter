# Milestone 4 test evidence

Last updated: June 30, 2026

## Requirement to evidence map

| Requirement / scenario | Exact test file | Exact test function or title | Observable behavior proven | Residual limitation |
| --- | --- | --- | --- | --- |
| M4-R01, M4-R02, Scenario 4.1.1 | `internal/agent/registry_test.go` | `TestLoaderStrictlyLoadsAndRejectsRegistryFiles` | Direct project-local `agents/*.yaml` and `styles/*.yaml` files load in deterministic order with strict single-document parsing. | Unit-level loader proof; real-project path is exercised separately below. |
| M4-R01, M4-R02, Scenario 4.1.1 | `internal/action/integration_test.go` | `TestMilestone4ActionFlowWithRealAdapters` | A created project with starter registries lists the expected built-in agents and styles through the real filesystem/Git/SQLite service stack. | Covers starter registries, not every malformed registry permutation. |
| M4-R03, Scenario 4.1.2 | `internal/agent/registry_test.go` | `TestLoaderStrictlyLoadsAndRejectsRegistryFiles`; `TestLoaderRejectsStyleWithoutRequiredTemperature` | Unknown fields, duplicate IDs, multiple YAML documents, symlinks, unsupported schemas, and missing required style temperature all fail closed without silent repair. | Registry failure mapping at the HTTP layer is covered separately. |
| M4-R04, M4-R05, Scenario 4.2.1, 4.2.2 | `internal/agent/model_test.go` | `TestRegistryValidationApplicabilityAndContext` | Applicability stays a pure decision over surface, scope, and word count, and exclusion reasons remain inspectable. | Public API only returns applicable actions; excluded reasons stay domain-only for now. |
| M4-R04, M4-R05, M4-R06, M4-R09, M4-R11, Scenario 4.2.1, 4.3.1, 4.3.2, 4.3.3 | `internal/action/service_test.go` | `TestServiceRunRejectAndAcceptFlow`; `TestServiceRejectsStaleSelectionsAndReleasesFailedAcceptClaims` | The run path rechecks applicability against canonical scene bytes, assembles only the allowed context, returns review data without canon mutation, and rejects stale revision or selected-text mismatches. | Service-level proof uses fakes for provider and patch acceptance boundaries. |
| M4-R06, Scenario 4.3.1 | `internal/agent/model_test.go` | `TestRegistryValidationApplicabilityAndContext` | Minimal context assembly includes `selected_text` and `style_sheet` and rejects forbidden optional packs. | Current proof is specific to the Milestone 4 supported context subset. |
| M4-R07, M4-R08, Scenario 4.3.2 | `internal/agent/provider_test.go` | `TestMockProviderProducesDeterministicReplacementAndHonorsCancellation` | Generation uses a provider-neutral request shape, returns deterministic mock output, and treats cancellation as provider unavailability. | Only the mock provider is in scope for Milestone 4. |
| M4-R08, M4-R10, M4-R11, Scenario 4.3.2 | `internal/action/service_test.go` | `TestRunRejectsByteIdenticalProviderOutputWithoutStoringRun` | A byte-identical provider replacement fails as `story.ErrNoSceneChanges` before any run ID is allocated or transient run is stored. | No persistence proof is needed because run insertion is blocked before store access. |
| M4-R09, Scenario 4.3.3 | `internal/story/scene_selection_test.go` | `TestValidateAndReplaceMarkdownSelection` | UTF-8 byte ranges are validated against canonical Markdown and only exact selected bytes are replaceable. | Low-level helper proof; service and UI layers cite it rather than duplicating byte math. |
| M4-R09, Scenario 4.3.3 | `web/src/editor/selection.test.ts` | `counts words and converts UTF-8 byte ranges for multibyte selections` | Frontend selection math converts CodeMirror character offsets into UTF-8 byte offsets correctly for multibyte text. | Boundary test coverage below proves the converted values are sent over fetch for one multibyte selection. |
| M4-R10, Scenario 4.3.2, 4.4.1 | `internal/action/service_test.go` | `TestRunStoreEvictsTerminalRunsAndRejectsLiveCapacity`; `TestRunStoreRejectsDuplicateRunIDs` | Transient memory keeps live runs, evicts only terminal runs at capacity, and rejects duplicate run IDs instead of replacing live review state. | Run persistence across process restarts remains out of scope by design. |
| M4-R11, Scenario 4.3.2 | `internal/action/integration_test.go` | `TestMilestone4ActionFlowWithRealAdapters` | Running a real-adapter mock action leaves scene bytes, index bytes, worktree state, and Git history unchanged before accept/reject. | Single happy-path project flow rather than exhaustive registry permutations. |
| M4-R12, Scenario 4.4.1 | `internal/action/service_test.go` | `TestServiceRunRejectAndAcceptFlow` | Reject transitions a pending run to rejected and later decisions conflict. | In-memory only; real-adapter no-mutation proof is below. |
| M4-R12, Scenario 4.4.1 | `internal/action/integration_test.go` | `TestMilestone4ActionFlowWithRealAdapters` | Reject leaves canonical scene bytes and Git history unchanged through the real adapters. | Single run path, not concurrent rejection races. |
| M4-R13, M4-R14, Scenario 4.4.2, 4.4.3 | `internal/story/scene_patch_rollback_service_test.go` | `TestAcceptScenePatchHandlesEveryPersistenceFailureBoundary` | Accept patch rollback restores canonical state across write, index, and checkpoint failures while keeping the run retryable. | Story-service boundary proof; action-service race proof is separate. |
| M4-R13, M4-R14, Scenario 4.4.2 | `internal/action/integration_test.go` | `TestMilestone4ActionFlowWithRealAdapters` | Accept replaces only the selected interior UTF-8 byte range, preserves scene metadata, rebuilds the index, creates exactly one `Accept AI patch <run-id>` commit, and leaves the worktree clean. | Real-adapter acceptance is proven for one representative scene flow. |
| M4-R15, Scenario 4.4.1, 4.4.2, 4.4.3 | `internal/action/service_test.go` | `TestConcurrentAcceptsAllowExactlyOneRunClaim`; `TestConcurrentAcceptAndRejectAllowExactlyOneTerminalDecision`; `TestServiceRejectsStaleSelectionsAndReleasesFailedAcceptClaims` | Accept/accept and accept/reject races allow exactly one terminal decision, conflicting callers get `ErrRunConflict`, and failed accepts release the run back to pending. | Proof is for the in-process transient run store specified by Milestone 4. |
| M4-R15, Scenario 4.4.1, 4.4.3 | `internal/action/integration_test.go` | `TestMilestone4ActionFlowWithRealAdapters` | Duplicate decisions, stale revision accepts, and dirty-worktree accepts do not mutate canon twice through the real adapters. | Does not attempt cross-process coordination because runs are intentionally transient. |
| M4-R16, Scenario 4.1.1, 4.1.2, 4.2.1, 4.3.2, 4.3.3, 4.4.1, 4.4.2 | `internal/api/action_handler_test.go` | `TestActionRoutesReturnExactJSONShapes`; `TestActionRouteStatusMapping`; `TestActionRoutesRejectMalformedContractInputs`; `TestActionRegistryLoadFailuresReturnInternalServerError` | Registry, availability, run, accept, and reject routes return the documented JSON shapes, reject malformed query/body contracts, set `Allow` headers, and map registry/conflict/not-found/service errors to the expected statuses. | HTTP-layer proof uses a service stub, not the real project adapters. |
| M4-R17, M4-R18, Scenario 4.5.1, 4.5.2 | `web/src/editor/SceneEditor.actions.boundary.test.tsx` | `runs, rejects, and accepts scene actions through the fetch boundary`; `disables actions for dirty drafts` | The editor discovers applicable actions, runs through fetch, moves focus to the named preview region, exposes context summary, copies replacement text, rejects without mutation, accepts without a second scene save, sends UTF-8 byte offsets, and disables AI actions for dirty drafts with a clear explanation. | Automated frontend proof does not replace a full manual assistive-technology audit. |
| M4-R19 | Repository command suite | `PATH=/home/linuxbrew/.linuxbrew/bin:$PATH GOCACHE=/tmp/storywriter-go-cache make check`; `PATH=/home/linuxbrew/.linuxbrew/bin:$PATH GOCACHE=/tmp/storywriter-go-cache /home/linuxbrew/.linuxbrew/bin/go test -race ./...`; `git diff --check`; `git status --short` | Milestones 0 through 4 remain green under the full check, race, diff-validation, and clean-status suites. | Command results are point-in-time evidence and must be rerun after later changes. |
| M4-R20 | Documentation artifacts | `.plans/milestone_4_status.md`; this file | Durable docs, scenario-to-test evidence, final command results, and residual risks are recorded together before Milestone 4 is declared complete. | Future remediation still requires new proving tests first if new gaps are found. |
